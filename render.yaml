services:
  # 1. Next.js Frontend (Web Interfeys)
  - type: web
    name: voice-ai-web
    env: node
    buildCommand: npm install && npm run build --workspace=web
    startCommand: npm run start --workspace=web
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: NEXT_PUBLIC_API_URL
        fromService:
          type: web
          name: voice-ai-api
          envVarKey: RENDER_EXTERNAL_URL

  # 2. NestJS API (Backend)
  - type: web
    name: voice-ai-api
    env: node
    buildCommand: npm install && npm run build --workspace=api
    startCommand: npm run start:prod --workspace=api
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: REDIS_URL
        fromService:
          type: redis
          name: voice-ai-redis
          property: connectionString
      - key: POSTGRES_DB
        fromDatabase:
          name: voice-ai-db
          property: database
      - key: POSTGRES_USER
        fromDatabase:
          name: voice-ai-db
          property: user
      - key: POSTGRES_PASSWORD
        fromDatabase:
          name: voice-ai-db
          property: password
      - key: POSTGRES_HOST
        fromDatabase:
          name: voice-ai-db
          property: host
      - key: POSTGRES_PORT
        fromDatabase:
          name: voice-ai-db
          property: port

  # 3. Python Worker (Ovoz generator)
  - type: worker
    name: voice-ai-worker
    env: python
    buildCommand: cd apps/worker && pip install -r requirements.txt
    startCommand: cd apps/worker && python -m uvicorn app.main:app --port $PORT --host 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
      - key: REDIS_URL
        fromService:
          type: redis
          name: voice-ai-redis
          property: connectionString
      # Maxfiy kalitlarni Render Dashboard (Environment) dan qo'lda kiritasiz:
      - key: AZURE_TTS_KEY
        sync: false
      - key: AZURE_TTS_REGION
        sync: false
      - key: S3_ACCESS_KEY
        sync: false
      - key: S3_SECRET_KEY
        sync: false
      - key: S3_BUCKET
        sync: false
      - key: S3_REGION
        sync: false

  # 4. Redis Xizmati
  - type: redis
    name: voice-ai-redis
    ipAllowList: [] # faqat ichki xizmatlar kira oladi
    plan: free

# 5. PostgreSQL Baza
databases:
  - name: voice-ai-db
    databaseName: voiceai
    user: voiceai
    plan: free
